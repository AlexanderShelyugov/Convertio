management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  endpoint:
    health:
      show-components: always
      show-details: always
springdoc:
  swagger-ui:
    path: /docs